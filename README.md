# Portfolio — Gabriel F. Bello

Static site built with Astro 5 + Tailwind CSS (Mono design system).

```bash
pnpm dev     # local dev at http://localhost:4321
pnpm build   # static output in dist/
```

The previous Angular v1 is archived in `legacy-angular/` (see tag `v1-angular-final`).
